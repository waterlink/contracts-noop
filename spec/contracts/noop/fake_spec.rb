require "contracts/noop/fake"

# NOTE kinda strange spec, just defines things, if they don't blow up, then everything works

load "./fixtures/contracts.ruby/spec/support.rb"
load "./fixtures/contracts.ruby/spec/fixtures/fixtures.rb"
